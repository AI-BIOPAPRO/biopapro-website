# react-simple-maps@3.0.0's peerDependencies range (React 16-18) predates
# React 19 but the library works fine with it in practice. Without this,
# `npm install` fails outright (including on Vercel's build step) with an
# ERESOLVE peer-dependency conflict.
legacy-peer-deps=true
